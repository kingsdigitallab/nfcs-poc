# Copy this file to .env (which is gitignored) and fill in your key.
# The value is injected at Vite build time — it is NOT read at runtime.

VITE_KCL_API_KEY=sk-your-key-here
VITE_EUROPEANA_API_KEY=your-europeana-key-here
